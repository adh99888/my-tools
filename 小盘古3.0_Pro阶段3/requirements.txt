# 创生种子v0.1 最小依赖
# 版本：创生纪元起始版
# 更新时间：2025-02-03

# 核心依赖（必须）
pydantic>=2.0.0,<3.0.0  # 类型安全数据验证
requests>=2.28.0,<3.0.0  # HTTP请求，用于LLM API调用

# 可选依赖（增强功能）
# python-dotenv>=1.0.0  # 环境变量管理（未来可能需要）
# pyyaml>=6.0  # YAML配置文件支持（未来可能需要）

# 开发依赖（测试和代码质量）
# pytest>=7.0.0  # 单元测试框架
# black>=23.0.0  # 代码格式化
# ruff>=0.1.0  # Python linter和格式化工具
# mypy>=1.0.0  # 静态类型检查

# 运行环境要求
# Python>=3.8